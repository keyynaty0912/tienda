import type {Metadata} from 'next';import {getProducts,getConfig} from '@/lib/catalog';import {firebaseConfigured,isDemo} from '@/lib/firebase-admin';import {paymentBlockReason} from '@/lib/payments';import {StoreProvider} from '@/components/store';import {Header,Footer} from '@/components/shell';import './globals.css';
export const metadata:Metadata={metadataBase:new URL(process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'),title:{default:'Key & Naty · Boutique infantil',template:'%s · Key & Naty'},description:'Boutique infantil en Colombia. Explora prendas para bebé, niña y niño, medidas por talla y disponibilidad.',robots:isDemo()?{index:false,follow:false}:{index:true,follow:true},icons:{icon:'/favicon.svg'}};
export const dynamic='force-dynamic';
export default async function RootLayout({children}:{children:React.ReactNode}){const [products,config]=await Promise.all([getProducts(),getConfig()]);return <html lang="es-CO"><body><StoreProvider products={products} config={config} demo={isDemo()} firebaseReady={firebaseConfigured()} checkoutDisabled={paymentBlockReason(config)}><Header/><main id="main" tabIndex={-1}>{children}</main><Footer/></StoreProvider></body></html>}
