import {NextResponse} from 'next/server';import {checkoutSchema} from '@/lib/schema';import {createOrder} from '@/lib/orders';import {getConfig} from '@/lib/catalog';import {paymentBlockReason} from '@/lib/payments';import {AppError} from '@/lib/errors';import {checkOrigin,readJson,failure,guestSession,limitRequest,user} from '@/lib/security';
export async function POST(req:Request){try{checkOrigin(req);const block=paymentBlockReason(await getConfig());if(block)throw new AppError(503,block);await limitRequest(req,'checkout',15);const data=checkoutSchema.parse(await readJson(req)),guest=await guestSession(),u=await user();return NextResponse.json(await createOrder(data,guest,u?.uid||null),{status:201})}catch(e){return failure(e)}}
